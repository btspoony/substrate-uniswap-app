
export const state = () => ({
})

export const getters = {
}

export const mutations = {
}

export const actions = {
  // async nuxtServerInit({ commit, dispatch }, { store, req }) {
  //   await dispatch('ensureSystemStatus', req)
  // }
}
